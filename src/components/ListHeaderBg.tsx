import s from './ListHeaderBg.module.css';

/** Fundo do cabeçalho da lista (851 × 345 du): degradê verde, luz menta, curvas de nível e grão. Tudo em código/SVG. */
export function ListHeaderBg() {
  return <div className={s.bg} aria-hidden />;
}
